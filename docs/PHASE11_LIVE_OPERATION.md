# Phase 11: 本番トーナメント（NIGHT BATTLE #01）実施手順書

最終更新: 2025-01-30

このドキュメントは、本番環境で「NIGHT BATTLE #01」トーナメントを安全に運用するための手順書です。

---

## 📋 目次

1. [目的と前提条件](#目的と前提条件)
2. [Step A: 本番開始前チェック（Preflight）](#step-a-本番開始前チェックpreflight)
3. [Step B: トーナメント開始（Go Live）](#step-b-トーナメント開始go-live)
4. [Step C: 開催中運用（1日1回）](#step-c-開催中運用1日1回)
5. [Step D: 終了処理](#step-d-終了処理)
6. [Step E: 振り返り & 次判断](#step-e-振り返り--次判断)
7. [重要事項](#重要事項)

---

## 目的と前提条件

### 目的
- Dry Run を踏まえ、初回の本番トーナメントを安全に運用する
- 技術的完成度ではなく「運用・体験・トラブル耐性」を検証する

### 前提条件
- Phase 10 の Dry Run は完了済み
- Supabase / Stripe / Arena / Admin フローは手動運用前提
- 自動化・拡張は行わない（このフェーズでは新機能を作らない）

---

## Step A: 本番開始前チェック（Preflight）

### A-1. Supabase 環境確認

#### 1. プロジェクト確認
- [ ] Supabase Dashboard で本番プロジェクト（SellRush 本番）に接続していることを確認
- [ ] プロジェクト URL が本番環境であることを確認

#### 2. ユーザー確認
- [ ] Admin ユーザーが存在し、`user_metadata.role = "admin"` が設定されている
- [ ] Creator ユーザー（参加予定者）が存在し、`user_metadata.role = "creator"` が設定されている
- [ ] 参加 Creator のメールアドレスをリストアップ

**確認 SQL**:
```sql
-- Admin ユーザー確認
SELECT id, email, raw_user_meta_data->>'role' as role
FROM auth.users
WHERE raw_user_meta_data->>'role' = 'admin';

-- Creator ユーザー確認
SELECT id, email, raw_user_meta_data->>'role' as role
FROM auth.users
WHERE raw_user_meta_data->>'role' = 'creator';
```

#### 3. テーブル状態確認
- [ ] `tournaments` テーブルが存在することを確認
- [ ] `tournaments` テーブルが空 or 正しい状態であることを確認（既存の `live` トーナメントがないこと）

**確認 SQL**:
```sql
-- 既存のトーナメント確認
SELECT id, title, slug, status, start_at, end_at
FROM tournaments
ORDER BY created_at DESC;
```

### A-2. トーナメント準備

#### 1. トーナメント作成
- [ ] `docs/PRODUCTION_DRY_RUN_SQL.md` の SQL を使用して NIGHT BATTLE #01 を作成
- [ ] `status = 'scheduled'` で作成
- [ ] `start_at` と `end_at` を適切に設定（72時間推奨）

**使用 SQL**:
```sql
INSERT INTO tournaments (
  id,
  title,
  slug,
  status,
  start_at,
  end_at,
  product_id,
  created_by
) VALUES (
  gen_random_uuid(),
  'NIGHT BATTLE #01',
  'night-battle-01',
  'scheduled',
  '<開始日時>',  -- 例: '2025-02-01 20:00:00+09'
  '<終了日時>',  -- 例: '2025-02-04 20:00:00+09'
  NULL,         -- 必要に応じて商品IDを設定
  '<ADMIN_USER_ID>'
);
```

#### 2. 表示確認
- [ ] Admin 画面（`/admin/arena/tournaments`）でトーナメントが表示される
- [ ] Admin 詳細ページ（`/admin/arena/tournaments/night-battle-01`）で情報が正しく表示される
- [ ] Creator Dashboard（`/dashboard`）でトーナメント情報が表示される（scheduled 状態）
- [ ] Arena Page（`/arena/night-battle-01`）が正常に表示される

### A-3. 決済安全チェック

#### 1. Stripe 環境確認
- [ ] Vercel の環境変数で `STRIPE_SECRET_KEY` がテストキー（`sk_test_...`）であることを確認
- [ ] 本番決済が走らないことを確認

**確認方法**:
- Vercel Dashboard → Project Settings → Environment Variables
- `STRIPE_SECRET_KEY` の値が `sk_test_...` で始まっていることを確認

#### 2. テスト購入確認（オプション）
- [ ] テストカードで1回購入を試し、正常に動作することを確認
- [ ] `orders` テーブルにレコードが追加されることを確認

---

## Step B: トーナメント開始（Go Live）

### B-1. 開始操作

#### 1. ステータス変更
- [ ] Supabase SQL Editor で `tournaments.status` を `scheduled` → `live` に変更
- [ ] 変更時刻を記録

**使用 SQL**:
```sql
UPDATE tournaments
SET status = 'live',
    updated_at = now()
WHERE slug = 'night-battle-01';
```

**記録項目**:
- 開始日時: [YYYY-MM-DD HH:MM:SS]
- 実施者: [名前]

### B-2. 即時確認（開始後5分以内）

#### 1. LP 確認
- [ ] `https://sellrush.vercel.app/` の Arena 表示が更新されている
- [ ] 「Tonight Battle」セクションにトーナメント情報が表示される

#### 2. Creator Dashboard 確認
- [ ] Creator アカウントでログインし、`/dashboard` を確認
- [ ] `CurrentTournamentCard` が「今夜のバトル参加中」系の表示になっている
- [ ] トーナメント名「NIGHT BATTLE #01」が表示される

#### 3. Arena Page 確認
- [ ] `/arena/night-battle-01` が正常に表示される
- [ ] ランキングが空でもエラーにならない
- [ ] トーナメント情報（タイトル、期間、ステータス）が正しく表示される

### B-3. 初期異常チェック（開始後30分以内）

#### 1. ランキング確認
- [ ] ランキングが空でも画面が落ちない
- [ ] API エラーが発生していない（Vercel Logs を確認）

#### 2. 不正検知確認
- [ ] `fraud_flags` テーブルに異常な増加がない
- [ ] 不正検知ロジックが正常に動作している

**確認 SQL**:
```sql
-- 不正検知フラグの確認
SELECT COUNT(*) as fraud_count
FROM fraud_flags
WHERE created_at > now() - interval '1 hour';
```

#### 3. ログ確認
- [ ] Vercel Logs でエラーが発生していない
- [ ] Supabase Logs で異常なクエリがない

---

## Step C: 開催中運用（1日1回）

### C-1. 毎日確認項目（推奨時刻: 午前中）

#### 1. 注文件数確認
- [ ] `orders` テーブルの新規注文数を確認
- [ ] 前日比で異常な増減がないか確認

**確認 SQL**:
```sql
-- 今日の注文件数
SELECT COUNT(*) as today_orders
FROM orders
WHERE created_at >= CURRENT_DATE
  AND created_at < CURRENT_DATE + interval '1 day';
```

#### 2. ランキング更新確認
- [ ] `/arena/night-battle-01` のランキングが正しく更新されている
- [ ] Creator Dashboard の順位表示が正しい

#### 3. 不正検知フラグ確認
- [ ] `fraud_flags` テーブルに新規フラグがないか確認
- [ ] フラグがある場合は内容を確認

**確認 SQL**:
```sql
-- 未レビューの不正検知フラグ
SELECT id, creator_id, reason, created_at
FROM fraud_flags
WHERE reviewed = false
ORDER BY created_at DESC;
```

#### 4. Creator からの問い合わせ確認
- [ ] メール / Slack などで Creator からの問い合わせがないか確認
- [ ] 問い合わせがある場合は対応

### C-2. 手動対応ルール

#### 1. 明らかな不正が発見された場合
- [ ] Creator に一時停止依頼を送信
- [ ] `fraud_flags` テーブルに記録
- [ ] Admin 画面で該当 Creator の注文を確認

#### 2. バグが発見された場合
- [ ] `docs/BUGLOG.md` に即記録（修正は後回し）
- [ ] バグの影響範囲を確認
- [ ] 緊急度に応じて対応を判断

---

## Step D: 終了処理

### D-1. 終了操作

#### 1. ステータス変更
- [ ] Supabase SQL Editor で `tournaments.status` を `live` → `finished` に変更
- [ ] 終了時刻を記録

**使用 SQL**:
```sql
UPDATE tournaments
SET status = 'finished',
    updated_at = now()
WHERE slug = 'night-battle-01';
```

**記録項目**:
- 終了日時: [YYYY-MM-DD HH:MM:SS]
- 実施者: [名前]

### D-2. 表示確認

#### 1. Creator Dashboard 確認
- [ ] Creator アカウントでログインし、`/dashboard` を確認
- [ ] トーナメントカードの表示が「終了」状態に変わる
- [ ] 最終順位が表示される

#### 2. Arena Page 確認
- [ ] `/arena/night-battle-01` が正常に表示される
- [ ] 終了状態のコピーが表示される
- [ ] 最終ランキングが表示される

---

## Step E: 振り返り & 次判断

### E-1. 記録

#### 1. レポート作成
- [ ] `docs/PHASE11_LIVE_REPORT.md` を作成
- [ ] 以下の情報を記載:
  - 実施期間（開始日時〜終了日時）
  - 参加 Creator 数
  - 総注文件数
  - 総売上金額（オプション）
  - 想定外だった挙動
  - 致命的だった問題 / 軽微な問題
  - Creator からのフィードバック
  - 運用上の課題

### E-2. 判断

#### 1. 次フェーズの選択
以下のいずれかを判断:

**A. 同じ運用で #02 を実施**
- 条件: 致命的な問題がなく、同じ運用で改善できる
- アクション: Phase 11 を再実行（トーナメント名を #02 に変更）

**B. UX 改修してから再実施**
- 条件: UX の問題が運用に大きく影響している
- アクション: Phase 11-B（UX 改修）を実施後、Phase 11 を再実行

**C. 自動化フェーズ（Phase 12）へ進む**
- 条件: 運用が安定し、自動化の価値が明確
- アクション: Phase 12（自動化）を開始

---

## 重要事項

### このフェーズでやらないこと
- ❌ 新機能の実装
- ❌ UI の大幅な改修
- ❌ 自動化の実装
- ❌ スケール対応

### このフェーズでやること
- ✅ 安全性の確認
- ✅ 運用プロセスの検証
- ✅ ユーザー体験の検証
- ✅ トラブル耐性の検証
- ✅ 現実的な課題の洗い出し

---

## 関連ドキュメント

- [docs/PHASE10_FIRST_LIVE.md](docs/PHASE10_FIRST_LIVE.md): 初回トーナメント運用仕様
- [docs/PRODUCTION_DRY_RUN_SQL.md](docs/PRODUCTION_DRY_RUN_SQL.md): 本番 Dry Run 用 SQL クエリ
- [docs/PHASE10_RUNBOOK.md](docs/PHASE10_RUNBOOK.md): Admin Runbook
- [docs/BUGLOG.md](docs/BUGLOG.md): バグログ
- [docs/PHASE11_LIVE_REPORT.md](docs/PHASE11_LIVE_REPORT.md): 本番運用レポート（作成予定）

